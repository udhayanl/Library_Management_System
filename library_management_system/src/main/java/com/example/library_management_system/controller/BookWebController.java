package com.example.library_management_system.controller;

// This controller was removed because it conflicted with the REST API controller
// at /books. The API is handled by BookController, while the HTML pages are not
// currently used by the app startup flow. Keep the project to a single valid /books mapping.
public class BookWebController {
}